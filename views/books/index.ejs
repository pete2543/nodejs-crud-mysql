<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Overphone</title>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
</head>
<body class="container" style="margin-top: 50px;">

    <% if (messages.error) { %>
        <div class="alert alert-danger" role="alert"><%= messages.error %></div>
    <% } %>
    
    <% if (messages.success) { %>
        <div class="alert alert-success" role="alert"><%= messages.success %></div>
    <% } %>
    <h2 align="center"><img src="https://media.discordapp.net/attachments/383237359023685634/970275987529023508/over.jpg"></h2>

    <div class="card">
        <div class="card-header">
            <ul class="nav nav-pills w-100">
                <li class="nav-link">🏪Overphone</li>
                <li class="nav-link ml-auto">
                    <a href="/books/add" class="nav-link active">Add product</a>
                </li>
            </ul>
        </div>
        <div class="card-body">
            <% if(data.length) { %>
                <table class="table">
                    <thead>
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">category</th>
                            <th scope="col">product</th>
                            <th scope="col">price</th>
                            <th scope="col">GB</th>
                            <th scope="col">EDIT</th>
                            <th scope="col">DELETE</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for(let i = 0; i < data.length; i++) { %>
                            <tr>
                                <th scope="row"><%= data[i].id %></th>
                                <td><%= data[i].category %></td>
                                <td><%= data[i].product %></td>
                                <td><%= data[i].price %></td>
                                <td><%= data[i].GB %></td>
                                <td>
                                    <a href="../books/edit/<%=data[i].id %>" class="btn btn-warning edit">Edit</a>
                                   
                                </td>
                                <td>
                                    <a href="../books/delete/<%= data[i].id %>" onclick="alert('Are you sure?')" class="btn btn-danger delete">Delete</a>
                                </td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
                <a href="../books/show/" class="btn btn-success edit">Show</a>
            <% } %>
            <!-- if result is empty -->
            <% if(!data.length) { %>
                <p class="text-center">No book found!</p>
            <% } %>
        </div>
    </div>

</body>
</html>